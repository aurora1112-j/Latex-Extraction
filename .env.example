# Server-side Gemini API key.
# This key is read only by server.js and is no longer injected into the browser bundle.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Optional server port. Defaults to 3000.
PORT="3000"
